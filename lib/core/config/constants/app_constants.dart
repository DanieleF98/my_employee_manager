import 'package:flutter/material.dart';

abstract class AppConstants {
  static const double defaultIconSize = 32;
  static const double defaultBottomNavigationIconSize = 28;
  static const double defaultPadding = 24;
  static const double employeeCardIconSize = 60.0;
  static const Radius defaultBorderRadius = Radius.circular(
    20,
  );
}
